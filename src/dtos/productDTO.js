class ProductDTO {
  constructor(ProductName, content, author, password, status) {
    this.ProductName = ProductName;
    this.content = content;
    this.author = author;
    this.password = password;
    this.status = status;
  }
}

export default  ProductDTO;
